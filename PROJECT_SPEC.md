# 陶艺展览策划台

面向独立策展人的 Vue 3 展览编排工具。策展人可创建展览草稿、选择作品编排并上线展览状态。

## 工作流
1. 创建展览：校验名称非空，创建草稿并选中。
2. 编辑信息：修改副标题、策展人、开放日期、结束日期、说明并保存；五项必填，结束日期不得早于开放日期，校验失败提示原因且不写入。
3. 编排作品：切换作品加入状态，实时更新并持久化。
4. 上线展览：上线前检查至少一件作品，成功后状态转为“已上线”，失败显示原因。信息修改保存后立即生效，列表、详情、导览、分享四处同源一致。

## 模块与机制
作品档案、展览编排、本地持久化、导览信息。使用状态转换、边界校验、错误传播和 localStorage 生命周期。

测试按初始化基线约定延后，当前提供构建与工作流烟囱检查。
