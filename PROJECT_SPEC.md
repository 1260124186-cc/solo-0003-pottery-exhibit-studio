# 陶艺展览策划台

面向独立策展人的 Vue 3 展览编排工具。策展人可创建展览草稿、选择作品编排并上线展览状态。

## 工作流
1. 创建展览：校验名称非空，创建草稿并选中。
2. 编排作品与信息：切换作品、编辑副标题/策展人/展期/说明，实时更新并持久化；已上线展览锁定编辑。
3. 受控前进：完成度条件全部满足后，草稿 → 预览中 → 已上线，仅允许相邻一级，失败时保持原状态。
4. 受控回退：已上线 → 预览中、预览中 → 草稿，必须在界面明确二次确认；回退后再次前进仍走同一套完成度校验。

## 状态流转规则（domain/flow.ts 为唯一规则源）
- 状态顺序：草稿 < 预览中 < 已上线；只允许相邻状态间流转，跨级一律拒绝。
- 前进条件（进入预览与上线共用）：至少一件作品；副标题、策展人、展览说明非空；开幕日期不晚于闭幕日期。
- 回退：方向合法但必须 `confirmed: true`，未确认的回退不改状态、不写存储。
- 任何流转先校验后写入：抛错即保持原状态，列表与 localStorage 均不变；成功才持久化，刷新后一致。
- 完成度面板与前进门控同源于 `isReady/completionChecklist`，不会出现“面板说可以但状态改不了”。

## 模块与机制
作品档案、展览编排、本地持久化、导览信息。使用状态转换、边界校验、错误传播和 localStorage 生命周期。

- `npm run verify:flow`：状态流转规则验证（54 项断言，含刷新一致性子进程）。
- `npm run build`：类型检查与生产构建；另提供开发服务器烟囱检查。
