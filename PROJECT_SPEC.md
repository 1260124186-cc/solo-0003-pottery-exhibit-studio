# 陶艺展览策划台

面向独立策展人的 Vue 3 展览编排工具。策展人可创建展览草稿、选择作品编排并上线展览状态。

## 工作流
1. 创建展览：校验名称非空，创建草稿并选中；也可从「模板库」或侧栏模板入口从模板带入副标题、说明与作品编排，生成全新草稿。
2. 编排作品：切换作品加入状态，实时更新并持久化。
3. 上线展览：上线前检查至少一件作品，成功后状态转为“已上线”，失败显示原因。
4. 模板管理：独立入口查看、编辑（版本递增）、使用、删除（删除前确认）。模板可由展览“另存为模板”。

## 血缘关系
模板 → 由模板创建的草稿 → 草稿的副本，三者引用关系记录于展览 lineage（来源模板 id/版本/内容指纹/父草稿 id），工作面提供实时校验：模板修改或删除不影响已有草稿；深拷贝保证无共享引用；版本记录缺失、父草稿删除、共享数组引用分别报告为损坏/缺失。模板、草稿、副本均跨刷新持久化（同一 localStorage 命名空间，兼容旧数据迁移）。

测试：`npm run build` 生产烟囱检查；`npm run verify:lineage` 血缘端到端断言。
