# 陶艺展览策划台

面向独立策展人的 Vue 3 展览编排工具。策展人可创建展览草稿、选择作品编排并上线展览状态。

## 工作流
1. 创建展览：校验名称非空，创建草稿并选中。
2. 编排作品：切换作品加入状态，实时更新并持久化。
3. 空间编排：为展览内每件作品设置展厅、墙面位置与建议停留分钟，可上移下移调整呈现顺序；停留时间必须为正数，非法输入阻止保存并说明原因；作品加入或移出时空间信息自动保持一致。
4. 上线展览：上线前检查至少一件作品，成功后状态转为“已上线”，失败显示原因。

## 模块与机制
作品档案、展览编排、本地持久化、导览信息。使用状态转换、边界校验、错误传播和 localStorage 生命周期。

测试按初始化基线约定延后，当前提供构建与工作流烟囱检查（`npm run smoke` 覆盖空间编排的设置、校验、排序、恢复与作品增删路径）。
