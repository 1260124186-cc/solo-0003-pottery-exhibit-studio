# 陶艺展览策划台

面向独立策展人的 Vue 3 展览编排工具。策展人可创建展览草稿、选择作品编排并上线展览状态。

## 工作流
1. 创建展览：校验名称非空，创建草稿并选中。
2. 编排作品：切换作品加入状态，实时更新并持久化。
3. 上线展览：上线前检查完成度（基础信息齐备、至少一件作品、导览或空间信息齐备其一），成功后状态转为“已上线”，失败显示原因。

## 完成度面板
完成度面板与上线校验共享 `domain/readiness.ts` 这一套纯函数判定规则：面板只读呈现三态（空展览 / 尚未完成 / 已可上线）并逐项指出缺项，不能改变展览状态；上线动作按同一规则放行或拒绝。结果确定可复现，缺字段不报错。

## 模块与机制
作品档案、展览编排、本地持久化、导览信息。使用状态转换、边界校验、错误传播和 localStorage 生命周期。

测试按初始化基线约定延后，当前提供构建与工作流烟囱检查；完成度规则由 `npm run verify:readiness` 验证（对拍面板与上线动作、确定性、刷新一致、不改变状态）。
