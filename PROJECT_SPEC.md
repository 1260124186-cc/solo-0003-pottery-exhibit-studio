# 陶艺展览策划台

面向独立策展人的 Vue 3 展览编排工具。策展人可创建展览草稿、选择作品编排并上线展览状态。

## 工作流
1. 创建展览：校验名称非空，创建草稿并选中。
2. 编排作品：切换作品加入状态，实时更新并持久化。
3. 上线展览：上线前检查至少一件作品，成功后状态转为“已上线”，失败显示原因。
4. 全局搜索：单关键词同时匹配展览名称、副标题、策展人与作品标题、作者、材质，结果分组展示；输入防抖即时搜索或回车/按钮提交；点击结果跳转到对应展览（选中并滚动到工作面）或作品（滚动并高亮，不改选中）。搜索为只读操作，不修改数据与选中状态。

## 模块与机制
作品档案、展览编排、本地持久化、导览信息、全局搜索。使用状态转换、边界校验、错误传播和 localStorage 生命周期。

## 搜索机制
- 归一化：关键词与字段统一执行“去首尾空白（含全角空格）→ 连续空白折叠为单个半角空格 → 小写”，再按包含匹配；同一关键词的任意输入形式结果一致、可复现。
- 纯函数搜索（`src/domain/search.ts`）与响应式状态机（`src/composables/useGlobalSearch.ts`，idle/pending/ready）分离；执行器异步化并以单调递增序号丢弃过期结果，解决快速连续输入下旧结果晚到的竞态。

测试：`npm test`（vitest + @vue/test-utils + jsdom），覆盖字段搜索、无结果、归一化、竞态与跳转；`npm run build` 保留为三个策展工作流的生产烟囱检查。
