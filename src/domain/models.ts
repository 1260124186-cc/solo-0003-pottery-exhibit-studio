export type ExhibitStatus='草稿'|'预览中'|'已上线'; export interface Artwork{id:string;title:string;artist:string;material:string;year:number;note:string;tone:string}; export interface Exhibition{id:string;title:string;subtitle:string;curator:string;status:ExhibitStatus;opening:string;closing:string;pieces:string[];description:string}; export type HistoryKind='创建展览'|'加入作品'|'移出作品'|'上线展览'; export interface HistoryEntry{id:string;time:string;kind:HistoryKind;exhibit:string;detail:string}
