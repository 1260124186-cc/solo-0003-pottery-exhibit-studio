export type ExhibitStatus='草稿'|'预览中'|'已上线'; export interface Artwork{id:string;title:string;artist:string;material:string;year:number;note:string;tone:string}; export interface Placement{hall:string;wall:string;minutes:number}; export interface Exhibition{id:string;title:string;subtitle:string;curator:string;status:ExhibitStatus;opening:string;closing:string;pieces:string[];placements:Record<string,Placement>;description:string}
