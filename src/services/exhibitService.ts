import {ref} from 'vue';import {exhibitions,artworks} from '../domain/seed';import {buildExhibitionExport,exportFileName} from './exhibitExport';const key='pottery-exhibit-studio-v1';const saved=localStorage.getItem(key);const state=ref(saved?JSON.parse(saved):{exhibitions:[...exhibitions],artworks:[...artworks]});function persist(){localStorage.setItem(key,JSON.stringify(state.value))};export function useExhibitService(){const create=(title:string)=>{if(!title.trim())throw Error('展览名称不能为空');const e={id:'e'+Date.now(),title:title.trim(),subtitle:'新的展览叙事',curator:'未署名',status:'草稿',opening:'2025-01-01',closing:'2025-03-01',pieces:[],description:'等待策展人补充展览说明。'};state.value.exhibitions.unshift(e);persist();return e};const release=(id:string)=>{const e=state.value.exhibitions.find((x:any)=>x.id===id);if(!e||!e.pieces.length)throw Error('至少编排一件作品后才能上线');e.status='已上线';persist()};const togglePiece=(id:string,art:string)=>{const e=state.value.exhibitions.find((x:any)=>x.id===id);if(!e)return;e.pieces=e.pieces.includes(art)?e.pieces.filter((x:string)=>x!==art):[...e.pieces,art];persist()};const exportExhibition=(id:string)=>{const e=state.value.exhibitions.find((x:any)=>x.id===id);if(!e)throw Error('未找到要导出的展览');return{payload:buildExhibitionExport(e,state.value.artworks),filename:exportFileName(e)}};return{state,create,release,togglePiece,exportExhibition}}
