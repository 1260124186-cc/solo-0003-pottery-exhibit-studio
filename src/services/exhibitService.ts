import {ref} from 'vue';import {exhibitions,artworks} from '../domain/seed';import {validateArtwork} from '../domain/artwork';const key='pottery-exhibit-studio-v1';const saved=localStorage.getItem(key);const state=ref(saved?JSON.parse(saved):{exhibitions:[...exhibitions],artworks:[...artworks]});function persist(){localStorage.setItem(key,JSON.stringify(state.value))};export function useExhibitService(){const create=(title:string)=>{if(!title.trim())throw Error('展览名称不能为空');const e={id:'e'+Date.now(),title:title.trim(),subtitle:'新的展览叙事',curator:'未署名',status:'草稿',opening:'2025-01-01',closing:'2025-03-01',pieces:[],description:'等待策展人补充展览说明。'};state.value.exhibitions.unshift(e);persist();return e};const release=(id:string)=>{const e=state.value.exhibitions.find((x:any)=>x.id===id);if(!e||!e.pieces.length)throw Error('至少编排一件作品后才能上线');e.status='已上线';persist()};const togglePiece=(id:string,art:string)=>{const e=state.value.exhibitions.find((x:any)=>x.id===id);if(!e)return;e.pieces=e.pieces.includes(art)?e.pieces.filter((x:string)=>x!==art):[...e.pieces,art];persist()};const saveArtwork=(input:any,id?:string)=>{const errors=validateArtwork(input);if(Object.keys(errors).length){const err=Error('请完善作品信息后再保存')as any;err.fieldErrors=errors;throw err};const data={title:String(input.title).trim(),artist:String(input.artist).trim(),material:String(input.material).trim(),year:Number(input.year),note:String(input.note).trim(),tone:String(input.tone).trim()};if(id){const a=state.value.artworks.find((x:any)=>x.id===id);if(!a)throw Error('作品不存在');Object.assign(a,data);persist();return a};const a={id:'a'+Date.now(),...data,status:'启用'};state.value.artworks.push(a);persist();return a};const retireArtwork=(id:string)=>{const a=state.value.artworks.find((x:any)=>x.id===id);if(!a)return;a.status='停用';persist()};const includePiece=(id:string,art:string)=>{const e=state.value.exhibitions.find((x:any)=>x.id===id);if(!e||e.pieces.includes(art))return;e.pieces=[...e.pieces,art];persist()};return{state,create,release,togglePiece,saveArtwork,retireArtwork,includePiece}}
